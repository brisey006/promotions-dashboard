                    <div class="login-right-wrap wd-300">
                        <div class="lock-user">
                           <h4 class="mg-b-30 tx-semibold">Sign in</h4>
                        </div>
                        <!-- Form -->
                        <form method="POST">
                            <div class="form-group">
                                <input class="form-control" type="email" name="email" required placeholder="Email">
                            </div>
                            <div class="form-group">
                                <input class="form-control" type="password" name="password" required placeholder="Password">
                            </div>
                            {{>auth-errors}}
                            <div class="form-group mb-0">
                                <button class="btn btn-brand btn-block" type="submit">Sign in</button>
                            </div>
                        </form>
                        <!-- /Form -->
                        <div class="tx-gray-500 mg-t-10">New to Promotions? <a href="/register" class="tx-semibold">Create Account</a></div>
                    </div>